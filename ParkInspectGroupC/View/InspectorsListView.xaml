<UserControl x:Class="ParkInspectGroupC.View.InspectorsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ParkInspectGroupC.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding InspectorList, Source={StaticResource Locator}}">
    <Canvas>
        <TextBlock Text="Inspecteurs" Canvas.Top="10" Canvas.Left="10" FontSize="18" FontWeight="Bold"/>
        <Button Content="Gegevens aanpasen" Command="{Binding ShowEditInspectorCommand}" FontWeight="Bold" FontSize="15" Canvas.Top="10" Canvas.Left="120"/>
        <Button Content="Availabality aanpassen" Command="{Binding ShowEditAvailabilityCommand}" FontWeight="Bold" FontSize="15" Canvas.Top="10" Canvas.Left="270"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Canvas.Top="40" Canvas.Left="10">

            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" SelectedItem="{Binding SelectedInspector}" ItemsSource="{Binding InspectorsList, UpdateSourceTrigger=PropertyChanged}" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Naam">
                        <DataGridTextColumn.Binding>
                            <MultiBinding StringFormat="{}{0} {1} {2}">
                                <Binding Path="FirstName"/>
                                <Binding Path="Prefix"/>
                                <Binding Path="SurName"/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        <Border Canvas.Top="40" Canvas.Left="200" Visibility="{Binding ShowAvailability, Converter={StaticResource ShowAvailability}}">
            <DataGrid Grid.ColumnSpan="2" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding InspectorAvailability, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column" Canvas.Top="30" Canvas.Left="10" CanUserResizeColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tijd" Width="150">
                        <DataGridTextColumn.Binding>
                            <MultiBinding StringFormat="{}{0}-{1}">
                                <Binding Path="StartTime"/>
                                <Binding Path="EndTime"/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Datum" Binding="{Binding Date}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Canvas>
</UserControl>
